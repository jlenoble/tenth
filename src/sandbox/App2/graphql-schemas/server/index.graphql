# import Item, ItemWithRelatedItems, Query, Mutation from "item.graphql"
# import Relation, Query from "relation.graphql"
# import User, Query from "user.graphql"

type Query {
  _empty: String @deprecated(reason: "")
}

type Mutation {
  _empty: String @deprecated(reason: "")
}