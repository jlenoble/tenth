# import Item, ItemWithRelatedItems, User from "index.graphql"

type Query {
  items: [Item!]!
  item(id: Int!): Item
  me: User

  relatedItems(relatedToId: Int!, relationType: String!): ItemWithRelatedItems
}