mutation CreateItem($title: String!) {
  createItem(title: $title) {
    id
    title
  }
}

mutation CreateRelatedItem($relatedToId: Int!, $relationId: Int!, $title: String!) {
  createRelatedItem(relatedToId: $relatedToId, relationId: $relationId, title: $title) {
    item {
      id
      title
    }
    relationship {
      id
      ids
    }
  }
}