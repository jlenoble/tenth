# import Query from "server/query.graphql"

query GetRelatedItems($relatedToId: Int!, $relationType: String!) {
  relatedItems(relatedToId: $relatedToId, relationType: $relationType) {
    id
    title
    items {
      id
      title
    }
  }
}