# import Item from "item.graphql"

type RelatedItem {
  # This part should match exactly Item
  id: Int!
  userId: Int!
  title: String!

  # This part extends Item
  relationType: String!
  relatedToId: Int!
}