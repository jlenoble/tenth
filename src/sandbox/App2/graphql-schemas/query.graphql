# import Item, ItemWithRelatedItems, User from "server/index.graphql"

type Query {
  items: [Item!]!
  item(id: Int!): Item
  me: User

  relatedItems(relatedToId: Int!, relationType: String!): ItemWithRelatedItems
}