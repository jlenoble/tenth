type Relation {
  id: Int!
  type: String!
  itemId1: Int!
  itemId2: Int!
}

extend type Query {
  relation(id: Int!): Relation
}

extend type Mutation {
  createRelation(relatedToId: Int!, relationType: String!, relatedId: Int!): Relation!
}